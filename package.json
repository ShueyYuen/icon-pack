{
  "name": "icon-pack",
  "private": true,
  "scripts": {
    "consumer:build": "pnpm run -C consumer build",
    "consumer:run": "pnpm run -C consumer consume",
    "consumer:dev": "pnpm run -C consumer dev",
    "site:build": "pnpm run -C site build",
    "site:dev": "pnpm run -C site dev",
    "site:generate": "pnpm run -C site generate",
    "site:preview": "pnpm run -C site preview",
    "site:postinstall": "pnpm run -C site prepare",
    "icon:build": "pnpm run -C icon build",
    "plugin:dev": "pnpm run -C plugin dev",
    "plugin:build": "pnpm run -C plugin build"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "latest"
  }
}
